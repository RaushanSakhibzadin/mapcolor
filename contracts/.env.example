# cp .env.example .env  — .env is gitignored, keep it that way.
# Generate a throwaway key, never reuse a wallet that holds anything:
#   cast wallet new
# Then get testnet MON for that address (at Blitz: ask the organizers).
PRIVATE_KEY=0x
RPC_URL=https://testnet-rpc.monad.xyz
TEAM_COUNT=4
